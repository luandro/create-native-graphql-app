{
  "private": true,
  "scripts": {
    "format": "prettier --single-quote --trailing-comma=es5 --print-width=100 --write '{create-native-graphql-app,native-graphql-scripts}/{src,template}/**/*.js'"
  },
  "devDependencies": {
    "prettier": "^0.21.0"
  }
}
